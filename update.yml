- hosts: localhost
  connection: localhost
  gather_facts: false
  become: yes
  tasks:
    - name: Update Server
      yum:
        name: '*'
        state: present  
    - name: Install NGINX
      yum:
        name: nginx
        state: present